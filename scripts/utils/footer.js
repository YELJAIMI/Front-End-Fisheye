

    /*// DOM footer
    // compteur de likes au clic         
    HeartFooter.addEventListener('click',function(e){
    e.preventDefault();   
    Likes();

    }) 

    // compteur de likes au clavier
    HeartFooter.addEventListener('keydown', function(e) {
    if (e.key === "Enter") {
    Likes();     
    }
     
    }) 
    
            
    /*function countertotalLikes(media) {
        // COMPTEUR total de likes  //
                  
        const medialikes = document.querySelectorAll('.numberlikes'); 
                  
                  
        let somme = 0;
        this.medialikes.forEach(function(media) {
            somme += media[i].likes;
        })
        
        textNumber.textContent = somme;
      }        
   
   
   
  async function displayData(filterMedia) {
                  
      photographerfactory();
      
      filterMedia.sort((a, b) => {
          let aDate = a.date;
          let bDate = b.date;
       
          if (aDate < bDate) {
              return -1;
           } else if (aDate == bDate) {
              return 0;
           } else {
              return 1;
           }
       
       });
      // créer un article pour chaque photo
      filterMedia.forEach((media) => {
          
          photographerfactory(media);         
          
      });
      // compteur de likes au clic         
           HeartFooter.addEventListener('click',function(e){
            e.preventDefault();   
            countertotalLikes();

      }) 

// compteur de likes au clavier
            HeartFooter.addEventListener('keydown', function(e) {
            if (e.key === "Enter") {
            countertotalLikes();     
         }
     
     })
  };*/
  
             
